Table: committee_user
Columns:
user_id bigint UN PK 
committee_id bigint UN PK 
role varchar(50) 
assigned_at timestamp

Table: event_participants
Columns:
id bigint UN AI PK 
user_id bigint UN 
event_id bigint UN 
status enum('pendiente','aprobado','rechazado') 
created_at timestamp 
updated_at timestamp

Prompt:  
Indica que en la tabla `committee_user` **no debe existir ni manejarse el campo `role`**, ya que dentro de los comités no existen roles diferenciados para los usuarios. Elimina todo atributo, lógica de backend o frontend, migración, modelo Eloquent y validación que haga referencia a un campo llamado `role` en la relación de comités con usuarios.

Además, si quieres referirte a participación específica por usuario dentro del evento, consulta la tabla `event_participants`.  
Aclara que la tabla `committee_user` solo debe almacenar la relación de pertenencia y la fecha de asignación (`assigned_at`) entre el usuario y el comité, sin ningún campo de rol.
Indica que en la tabla `event_participants` **no debe existir ni manejarse el campo `status`**, ya que los líderes de semillero, al integrarse a un evento, quedan automáticamente como participantes sin requerir un estado adicional. Elimina cualquier atributo, lógica de backend o frontend, migración, modelo Eloquent y validación que haga referencia al campo `status` en la relación de participantes de eventos.

La tabla `event_participants` solo debe registrar el vínculo entre usuario y evento junto con las fechas de creación/modificación (`created_at`, `updated_at`). No debe tener columnas para estados de participación.
